# ===================================
# PlanSight Backend Environment Variables
# Merged: Person B (Backend) + Person D (DevOps)
# ===================================

# === LLM Configuration (Required for AI features) ===
# Get your API key from: https://aistudio.google.com/app/apikey
LLM_API_KEY=your_gemini_api_key_here
LLM_MODEL=gemini-1.5-flash
# Alternative models: gemini-2.5-flash, gemini-1.5-pro

# === Ollama Configuration (Local AI for Execution Plan) ===
# Run: ollama pull gemini-3-flash-preview  (or whichever model you have)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=gemini-3-flash-preview

# === Voice/Audio (Optional - for future voice features) ===
ELEVENLABS_API_KEY=your_elevenlabs_key_here

# === CORS Configuration ===
# Frontend URLs that can access this API (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# === Cost Calculation Configuration ===
COST_RATE_PER_DEV_DAY=500.0
CURRENCY=USD

# === Server Configuration (Optional) ===
# Uncomment to customize server settings
# HOST=127.0.0.1
# PORT=8000
# DEBUG=true

# === Monte Carlo Simulation Settings (Optional) ===
# DEFAULT_SIMULATION_RUNS=1000
# MAX_SIMULATION_RUNS=5000

# === Logging Configuration (Optional) ===
# LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# === Performance Settings (Optional) ===
# TIMEOUT_SECONDS=30
# MAX_WORKERS=4
